# Scripts to build GEOS-Chem classic

1. Run ./install_libraries.sh to install compilers & libraries
2. Run ./set_bashrc.sh to configure environment
3. Run ./deploy_GC.sh to compile and configure GEOS-Chem classic